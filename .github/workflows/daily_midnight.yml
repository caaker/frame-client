name: Daily Script
on:
  schedule:
    - cron: '0 0 * * *'  # Runs at 00:00 UTC every day
  workflow_dispatch:      # Allows manual trigger for testing

# Grant permission to the GITHUB_TOKEN to push changes back to the repo
permissions:
  contents: write

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: Run Fetch Script
        run: node _dist/daily_midnight.js

      - name: Commit and Push cache.txt
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: auto-update article cache [skip ci]"
          file_pattern: '_dist/cache.txt'
